# RealtorAgent Client

Web-only Flutter client for browsing listings from ReadAPI.

## Run

```bash
flutter run -d chrome --dart-define=READAPI_BASE_URL=http://127.0.0.1:8090
```

If `READAPI_BASE_URL` is not provided, the app defaults to `http://127.0.0.1:8090`.
